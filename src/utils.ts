export const greet = (input: string): string => {
  return input.toUpperCase();
};

export const greet2 = (input: string): string => {
  return input.toUpperCase();
};

export const greet3 = (input: string): string => {
  return input.toUpperCase();
};

export const greet4 = (input: string): string => {
  return input.toUpperCase();
};
